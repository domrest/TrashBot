To build, navigate to the directory and then in terminal 

cmake .
make
./Dashboard

The qt libraries should have been installed before running 

To install the QT5 development packages:

apt-get install qtdeclarative5-dev-tools
apt-get install libqcustomplot-dev